package com.github.jbescos.gameplay.roguelite;

/** Rendering hint for an active card ability. It does not participate in simulation logic. */
public enum RogueliteAbilityVisualStyle {
    NITRO,
    GRIP,
    RAM,
    DRAFT,
    SHIELD,
    MIRROR,
    CLOAK
}
